# Ember memory (never commit)
private_vault/**


# Local AI artifacts
models/
logs/
cache/

# UAT runner output (history of automated runs)
uat_results/

# ADR-037 classifier-label extraction output (privacy: queries may
# contain personal content; gitignored until human-reviewed and
# paraphrased into classifier_examples.py)
extract_classifier_labels_output/

# Environment files
.env
.env.local

# Watchdog signal files (runtime only, never committed)
ember_restart.signal
ember_stop.signal

# Python
.venv/
.env
__pycache__/
*.pyc

# Built UI (populated by installer from ember-2-ui)
ui/

# One-time cleanup scripts (not part of the codebase)
scripts/cleanup_today_test_sessions.py

# Local dev tools
watch-cc.ps1

# OS junk
.DS_Store
Thumbs.db

# Your Momgit add .
