version: "3.8"

services:
  searxng:
    image: searxng/searxng:latest
    container_name: ember-searxng
    ports:
      - "127.0.0.1:8888:8080"
    volumes:
      - ./config/searxng:/etc/searxng
    restart: unless-stopped
