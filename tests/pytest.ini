[pytest]
testpaths = tests
python_files = test_*.py
addopts = -m "not eval"
markers =
    eval: Tier 4 conversation quality eval — requires live Ollama and Anthropic API. Run explicitly with: pytest tests/eval/ -m eval